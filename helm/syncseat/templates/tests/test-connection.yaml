apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "syncseat.fullname" . }}-test-connection"
  labels:
    {{- include "syncseat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "syncseat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
