apiVersion: v1
kind: Service

metadata:
  name: {{ include "booking.fullname" . }}
  labels:
    {{- include "booking.labels" . | nindent 4 }}

spec:
  type: {{ .Values.service.type }}

  selector:
    {{- include "booking.selectorLabels" . | nindent 4 }}

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP