# Default values for mail.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: docker.io/phanivarun/syncseatapp-mail
  # This sets the pull policy for images.
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"


# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # Configured to map the HTTP Node.js web listener rather than a legacy Go gRPC port
  port: 3002

# Application layer environment configurations mapped cleanly into the ConfigMap
config:
  # Sets the environment shell port variable (process.env.PORT)
  port: "3002"

# Structural environment dependencies used by bullmq.js, redisclient.js, and supabase.js
# These values are designed to map from the parent umbrella chart's global context
secret:
  enabled: true
  # Must perfectly match the name of the kubectl secret we created in Step 2
  existingSecret: "syncseat-mail-secret"
  # Tells the deployment manifest which internal key inside the secret holds the file
  key: "config"

# InitContainers hold the deployment sequence until essential backing infrastructure is warm
initContainers:
  enabled: true
  dependencies:
    - name: postgres
      port: 5432
    - name: redis
      port: 6379

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi



podDisruptionBudget:
  enabled: false
  minAvailable: 1

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70
  
nodeSelector: {}

tolerations: []

affinity: {}
